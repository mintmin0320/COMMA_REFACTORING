import "@testing-library/jest-dom";
import { render } from "@testing-library/react";
import LoginForm from './LoginForm';

describe("로그인 테스트", () => {
  test("로그인이 실패하면 에러메시지가 표시된다.", () => {
    // render(<LoginForm />);
  })
})