import styled from 'styled-components';

// CSS
const PaginationBox = styled.div`
  width: 100%;
  height: 50px;
  display: flex;
  justify-content: center;
  align-items: center;
`;

function PostPagination() {
  return (
    <PaginationBox>PostPagination</PaginationBox>
  );
};

export default PostPagination;